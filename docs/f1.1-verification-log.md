# F1.1 Verification Log

> **Date:** 2026-06-04
> **Executor:** _[your name]_
> **Source plan:** `specs/plans/PLAN-railway-provisioning.md`
> **Status:** _PENDING — fill in as checks complete_

## Purpose

This log is the durable record that the F1.1 platform-exists-and-works milestone was met. It captures the result of all 12 on-Railway verifications called out in the plan's T2 task spec, with timestamps, captured output, and the deploy ID for the persistence test. It replaces ad-hoc commit-message notes and survives future re-runs.

## How to fill this in

Most checks have a copy-paste command listed. Run the command, capture the output, paste it under "Actual result," and tick the PASS/FAIL box. For checks that require a browser (e.g., opening the Railway dashboard), describe what you see in plain text and record the relevant ID/URL.

The two helper scripts in `scripts/` automate the bulk of the work:

| Script | Purpose |
|--------|---------|
| `scripts/provision_railway.sh` | Idempotently provisions the project, service, Postgres, and Volume. Stops on `unifleet` project-name collision per the plan's open-question default. |
| `scripts/run_f1_1_verifications.sh` | Runs all 12 checks in order, captures output, and appends to this log. Halts on the first failure (an early failure invalidates later checks). |

Recommended order:
1. `bash scripts/provision_railway.sh` — provision everything
2. Manually verify the GitHub integration is linked (this requires a browser step the CLI cannot automate)
3. `bash scripts/run_f1_1_verifications.sh` — run the checks; it will print the exact commands to paste into a Railway shell for the few checks the CLI cannot automate
4. Manually fill in any remaining sections in this file
5. Commit the completed log

## Project metadata

| Field | Value |
|-------|-------|
| Railway account email | _[fill in]_ |
| Workspace | _[fill in, e.g., personal or team name]_ |
| Project name (locked) | `unifleet` |
| Project ID | _[fill in from dashboard, e.g., `6a14b05c-...`]_ |
| Service name (locked) | `web` |
| Service ID | _[fill in]_ |
| Postgres name (locked) | `unifleet` |
| Postgres ID | _[fill in]_ |
| Postgres region | `asia-southeast` (Singapore) |
| Volume name (locked) | `data` |
| Volume ID | _[fill in]_ |
| Volume mount path (locked) | `/data` |
| Volume size | 8 GB (Hobby default) |
| Linked GitHub repo | `kayacohen/unifleet-v2-webapp` |
| First deploy commit | `902c2dd feat(infra): F1.1 T1 — Railway deploy scaffold` |

---

## 1. Resource provisioning

### 1.1 `check_project_unifleet_exists`

> GIVEN the Railway dashboard is open WHEN the projects list is loaded THEN a project named exactly `unifleet` exists.

**Expected:** One project with the name `unifleet`. No collisions, no `unifleet-1`, no `unifleet-v2`.

**How to check (CLI):** `railway list`

**Actual result:**
```
[paste `railway list` output here]
```

- [ ] **PASS** — project exists with the exact name
- [ ] **FAIL** — collision; halt T2, follow the plan's "Project name collision" resolution path

**Project ID:** _[fill in]_

---

### 1.2 `check_service_web_provisioned`

> GIVEN the `unifleet` project is open WHEN the service list is loaded THEN a service named exactly `web` exists.

**Expected:** One service named `web`, with a Railway-provided public URL of the form `web-production-XXXX.up.railway.app`.

**How to check (CLI):** `railway status`

**Actual result:**
```
[paste `railway status` output here]
```

- [ ] **PASS** — service exists with the exact name
- [ ] **FAIL** — service missing or misnamed; check `scripts/provision_railway.sh` step 3

**Service ID:** _[fill in]_

**Public URL:** _[fill in, e.g., `https://web-production-XXXX.up.railway.app`]_

---

### 1.3 `check_postgres_unifleet_provisioned`

> GIVEN the `unifleet` project is open WHEN the database list is loaded THEN a Postgres database named exactly `unifleet` exists AND its region is `asia-southeast` (Singapore).

**Expected:** One Postgres database named `unifleet`, region `asia-southeast`.

**How to check (CLI):** Dashboard → project → Postgres tile. The CLI does not yet expose database region in `railway status`.

**Actual result:**
```
[describe what you see in the dashboard]
```

- [ ] **PASS** — DB exists in `asia-southeast`
- [ ] **FAIL** — DB missing, misnamed, or in wrong region; check `scripts/provision_railway.sh` step 4

**Database ID:** _[fill in]_

---

### 1.4 `check_volume_data_attached`

> GIVEN the `unifleet` project is open WHEN the volume list is loaded THEN a volume named `data` exists AND it shows as "Attached" to the `web` service AND its mount path is exactly `/data` AND its size is 8 GB (Hobby default).

**Expected:** One volume named `data`, attached to the `web` service, mount path `/data`, 8 GB.

**How to check (CLI):** `railway volume list`

**Actual result:**
```
[paste `railway volume list` output here]
```

- [ ] **PASS** — volume exists, attached, mount path `/data`, size 8 GB
- [ ] **FAIL** — volume misnamed, unattached, wrong mount path, or wrong size; check `scripts/provision_railway.sh` step 5

**Volume ID:** _[fill in]_

---

## 2. Deploy pipeline

### 2.1 `check_github_integration_linked`

> GIVEN the `web` service's settings WHEN the "Source" / "Deploy" section is opened THEN the GitHub repository is listed AND auto-deploy on push to `main` is enabled.

**Expected:** The repo `kayacohen/unifleet-v2-webapp` is listed as the source, and the trigger is set to "Push to main" (or equivalent).

**How to check:** Dashboard → `unifleet` project → `web` service → Settings → Source. This step requires a browser; the CLI cannot fully automate it.

**Actual result:**
```
[describe what you see in the dashboard]
```

- [ ] **PASS** — repo linked, auto-deploy on push to main enabled
- [ ] **FAIL** — repo not linked or auto-deploy disabled; follow the Railway GitHub-integration flow in the dashboard

**Linked repo:** _[fill in]_

---

### 2.2 `check_first_deploy_succeeds`

> GIVEN a no-op commit is pushed to `main` WHEN the Railway dashboard is observed THEN a new deploy starts within 30 seconds AND the deploy reaches a "Success" state within 3 minutes.

**Note for F1.1:** The T1 commit (`902c2dd`) is the first deploy. It is already on `origin/main` from the T2 prep. Linking GitHub in the dashboard will trigger the first deploy automatically. No no-op commit is needed.

**Expected:** A deploy starts within 30 seconds of the GitHub link, and reaches "Success" within 3 minutes (most builds complete in 1-2 minutes).

**How to check (CLI):** `railway logs` (streaming) or `railway deployment list` (snapshot)

**Actual result:**
```
[paste `railway logs` output here]
```

- [ ] **PASS** — deploy succeeded
- [ ] **FAIL** — deploy failed; check logs for missing dep, lock-file error, or build-command typo

**Deploy ID:** _[fill in]_

**Deploy start timestamp:** _[fill in, e.g., `2026-06-04T20:05:00Z`]_

**Deploy end timestamp:** _[fill in]_

---

## 3. Application contract

### 3.1 `check_sanity_app_responds_200_on_get`

> GIVEN the `web` service is deployed WHEN `curl -i <railway-provided-url>/` is run from a developer shell THEN the response status is 200 AND the response body is non-empty.

**How to run:**
```bash
RAILWAY_URL="https://web-production-XXXX.up.railway.app"  # from check 1.2
curl -i -s "$RAILWAY_URL/"
```

**Expected:** HTTP/1.1 200 OK, Content-Type text/html, body is the literal string `ok` (2 bytes).

**Actual result:**
```
[paste `curl -i` output here]
```

- [ ] **PASS** — status 200, body non-empty
- [ ] **FAIL** — status != 200 or empty body; check that the start command binds to `0.0.0.0:$PORT` in `railway.toml`

---

### 3.2 `check_sanity_app_responds_200_on_head`

> GIVEN the same URL WHEN `curl -I <railway-provided-url>/` is run THEN the response status is 200.

**How to run:**
```bash
RAILWAY_URL="https://web-production-XXXX.up.railway.app"
curl -I -s "$RAILWAY_URL/"
```

**Expected:** HTTP/1.1 200 OK

**Actual result:**
```
[paste `curl -I` output here]
```

- [ ] **PASS** — status 200
- [ ] **FAIL** — status != 200; check `sanity_app.py`'s route accepts HEAD (it should, by default)

---

## 4. Build probe

### 4.1 `check_build_probe_passes_on_railway`

> GIVEN a Railway shell is open WHEN `python scripts/verify_build.py` is run THEN exit code is 0 AND stdout contains `RESULT: PASS` AND every required dep is reported as `PASS` (not `SKIP`, not `FAIL`).

**How to run (CLI):**
```bash
railway run python scripts/verify_build.py
```

**How to run (dashboard):** Open the `web` service's "Shell" tab and run the same command.

**Expected:**
```
PASS: Flask
PASS: Pillow
PASS: reportlab
PASS: qrcode
PASS: pandas
PASS: pytz
PASS: psycopg
PASS: db (5.0s timeout, SELECT 1 = 1)
RESULT: PASS
```
Exit code 0.

**Actual result:**
```
[paste full stdout here]
```

- [ ] **PASS** — all 7 deps PASS, db PASS, `RESULT: PASS` on last line
- [ ] **FAIL** — at least one dep missing or db failed; see failure-mode table in the plan's "Build probe" section

---

### 4.2 `check_build_probe_connects_to_postgres`

> GIVEN the probe output from the previous check WHEN the per-dep results are inspected THEN the `db` / `psycopg` line shows `PASS` (not `SKIP`), confirming `$DATABASE_URL` was auto-injected and `SELECT 1` succeeded.

**How to check:** Look at the `db` line in the output from check 4.1.

**Expected:** `PASS: db` (not `SKIP: db`).

**Actual result:**
```
[paste the `db` line from the probe output]
```

- [ ] **PASS** — db check is PASS
- [ ] **FAIL** — db check is SKIP (DATABASE_URL not auto-injected) or FAIL (connection problem)

---

## 5. Volume persistence

### 5.1 `check_volume_mount_path_is_data`

> GIVEN a Railway shell WHEN `mount | grep data` is run THEN the output includes a mount at `/data` (sanity check; the bigger test is the next one).

**How to run (CLI):**
```bash
railway ssh -- mount | grep data
```

**Expected:** A line containing `/data` (e.g., `overlay on /data type overlay (...)`).

**Actual result:**
```
[paste `mount | grep data` output here]
```

- [ ] **PASS** — `/data` is mounted
- [ ] **FAIL** — `/data` not mounted; check the Volume's mount path in the dashboard (should be `/data` exactly)

---

### 5.2 `check_volume_persists_across_redeploy`

> GIVEN a Railway shell WHEN the following sequence is run in order:
> 1. `printf "f1.1-persistence-test-$(date +%s)" > /data/persistence_marker.txt`
> 2. `cat /data/persistence_marker.txt` (record the content)
> 3. Trigger a redeploy of the `web` service
> 4. Wait for the new deploy to reach "Success"
> 5. Open a fresh Railway shell and run `cat /data/persistence_marker.txt`
> THEN the marker content from step 5 must match the marker content from step 2 exactly.

**How to run (CLI):**
```bash
# Step 1 + 2: write and read the marker
railway ssh -- 'printf "f1.1-persistence-test-$(date +%s)" > /data/persistence_marker.txt && cat /data/persistence_marker.txt'
# Note the output as "original content."

# Step 3: trigger a redeploy
railway redeploy

# Step 4: wait for the new deploy to reach "Success" (watch with `railway logs`)

# Step 5: re-read the marker from a fresh shell
railway ssh -- cat /data/persistence_marker.txt
# Note the output as "post-redeploy content." It must equal "original content."
```

**Expected:** `original content` and `post-redeploy content` are byte-for-byte identical.

**Original content:** _[fill in, e.g., `f1.1-persistence-test-1717521900`]_

**Redeploy ID:** _[fill in, e.g., `dpl_abc123`]_

**Post-redeploy content:** _[fill in]_

- [ ] **PASS** — both contents match exactly
- [ ] **FAIL** — contents differ; the mount is wrong, the Volume is not persistent, or the redeploy was a config-reload not a full restart. Do not mark F1.1 done. Diagnose via dashboard.

---

## 6. Postgres connectivity

### 6.1 `check_psql_select_1_succeeds`

> GIVEN a Railway shell WHEN `psql "$DATABASE_URL" -c "SELECT 1"` is run THEN the output is `1` (or a tabular result with `1` in the first column) AND the exit code is 0.

**How to run (CLI):**
```bash
railway run psql "$DATABASE_URL" -c "SELECT 1"
# or, more directly:
railway connect  # opens an interactive psql shell; then type: SELECT 1;
```

**Expected:** A single row with `?column?` header and `1` value, or just `1` on its own line. Exit code 0.

**Actual result:**
```
[paste `psql` output here]
```

- [ ] **PASS** — `SELECT 1` returns `1`
- [ ] **FAIL** — connection refused, auth failed, or wrong DATABASE_URL; check Postgres provisioning and env-var injection

---

## Summary

- [ ] All 12 checks passed
- [ ] T2 complete; F1.1 ready for sign-off

**Excluded deviations from the plan (if any):** _[list any deviations from the locked names, region, or mount path; the plan's open-questions section allows these only as documented exceptions]_

**Open issues to file before sign-off:** _[list any checks that passed with caveats, or any checks deferred to a follow-up]_

**Sign-off:**
- [ ] F1.1 platform ready for application deployment (F1.6 + Phase 2)
- Executed by: _[name]_
- Date: _[date]_
